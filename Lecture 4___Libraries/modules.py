# modules means libraries
# modules: reusing or sharing the code without copy-paste
# Modules are simply files with the “.py” extension containing Python code that can be imported inside another Python Modules Operations Program.


# import: that keyword allow us to import those modules
# import random.choice(seq)


# module like a folder